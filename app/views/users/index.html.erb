<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <!--
  <% @users.each do |user| %>
 	<%= render user %>
  <% end %>
	-->
	<!-- 
	autimatically iterates over the users and 
	renders each one with a partial _user.html.erb
	-->
  <%= render @users %>
</ul>

<%= will_paginate %>